<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme fixo em claro (ignora tema do sistema). -->
    <style name="Base.Theme.Drive_20android" parent="Theme.Material3.Light.NoActionBar" />
    <style name="Theme.Drive_20android" parent="Base.Theme.Drive_20android" />

    <style name="CircularImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="RoundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item> <!-- ajuste o valor para mais ou menos arredondado -->
    </style>

</resources>